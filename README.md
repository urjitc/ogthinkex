old version of thinkex :)

to run:
vscode File > Add Folder to Workspace for both web and api folder
open terminal for each 
command to run api: uvicorn main:app --reload
command to run web: pnpm run dev
